import * as React from 'react';
import Button from '../Button';

const Header = () => {
    return (
        <header>
            <h1>To-Do-Ist</h1>
            < Button />
        </header>
    );
};

export default Header;